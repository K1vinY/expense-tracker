// Firestore 安全規則 - 測試版本
// 請將這些規則複製到 Firebase Console → Firestore Database → Rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 暫時允許所有已登入用戶讀取和寫入（測試用）
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
